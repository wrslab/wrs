# `wrs` API index

Prefer these in-house utilities over trimesh / scipy / open3d / fcl. Auto-generated by `tools/gen_api_index.py` (static ast scan); re-run after adding functions.

## `wrs.agent.cli_client`
_Drive the planner through the Claude Code CLI -- no API key needed._

- **class `ClaudeCodeClient`** — ``anthropic``-shaped client backed by ``claude -p`` (subscription).

## `wrs.agent.executor`
_Execute one generated plan script against a prepared scene namespace._

- `run_script(code, namespace)` — Run ``code`` against a copy of ``namespace``; return the Outcome.
- **class `Outcome`** — What one script run produced; falsy-``ok`` outcomes carry the why.
  - methods: `ok`, `describe`

## `wrs.agent.harness`
_The write -> run -> diagnose -> revise loop: an LLM plans with the skills._

- `default_client()` — The best LLM client this machine can offer, in preference order:
- `solve(task, build_scene, *, check=None, model='claude-opus-4-8', max_rounds=3, client=None, log=print)` — Decompose ``task`` into a plan over a scene, revising on failure.
- **class `Solution`** — What ``solve`` produced: the final outcome plus the revision history.

## `wrs.collider.aabb_collider`

- **class `AABBCollider`** — OBB/SAT batch collider based on ColliderBase pair compilation.
  - methods: `is_collided`

## `wrs.collider.collider_base`

- **class `ColliderBase`**
  - methods: `append`, `compile`, `actors`, `is_collided`

## `wrs.collider.collision_batch`

- `compute_wd_obb_batch(local_mins, local_maxs, tfs)` — local_min/local_max: (n,3)
- `compute_wd_aabb_batch(local_mins, local_maxs, tfs)`
- **class `CollisionBatch`**
  - methods: `update_transforms`

## `wrs.collider.cpu_simd`
_CPU triangle-mesh collision detection (numpy/SIMD). create_detector +_

- `create_detector(eps=1e-09, max_points=200)`
- `build_batch(items, pairs)`
- `cols_to_vfs(cols)`
- `cols_to_vffns(cols)`
- `cols_to_tris(cols)`
- `compute_aabb(tris)`
- `aabb_intersect(min_a, max_a, min_b, max_b)`
- `compute_triangle_planes(tris)` — plane through origin: n dot x = 0
- `point_in_tri_batch(pts, tris, eps=1e-09)`
- **class `CPUDetector`**
  - methods: `detect_collision`, `detect_collision_batch`

## `wrs.collider.gpu_broadphase`
_Two-kernel broad/narrow driver for the batched triangle collider._

- `bind_group_layout(device)`
- `max_poses_per_run(tri_total)`
- **class `BroadPhase`**
  - methods: `run`

## `wrs.collider.gpu_collision_batch`
_wgpu storage buffers for the batched triangle collider._

- **class `WgpuCollisionBatch`**
  - methods: `broadphase`, `snapshot_transforms`, `upload_poses`, `clear_counter`, `copy_results_to_staging`, `read_staged`, `release`

## `wrs.collider.gpu_simd_batch`
_GPU batch triangle-mesh collision on wgpu compute._

- `create_detector()`
- `build_batch(items, pairs)`
- **class `WgpuBatchDetector`**
  - methods: `detect_collision_batch`, `detect_collision_multi`

## `wrs.collider.mj_collider`

- **class `MJCollider`** — The robot's collision world: the planned mechanism(s) (``actors``), the
  - methods: `append`, `compile`, `refresh`, `exclude`, `include`, `is_collided`, `get_slice`, `set_mecba_qpos`, `save`, `actors`

## `wrs.collider.raycast`

- `ray_shoot_scene_object(scene_obj, orig, direction)` — Ray cast against all collision shapes of a SceneObject.

## `wrs.collider.tbd_collider`

- `is_collided(sobj_a, sobj_b, eps=1e-09, max_points=1000)` — detect collision between two scene objects

## `wrs.collider.tbd_merged_geom`

- **class `MergedGeom`** — Merged collision geom built from multiple collision shapes.
  - methods: `from_collisions`, `vertices`, `faces`, `aabb`, `get_vertex_buffer`, `get_face_buffer`
- **class `MergedGeomBatch`** — Batch data for GPU collision queries.
  - methods: `merged_geoms`, `pair_indices`, `num_geoms`, `num_pairs`, `get_vertices`, `get_faces`, `get_geom_desc`, `get_pairs`

## `wrs.collider.tbd_simd_collider`

- **class `SIMDCollider`** — SIMD-based collision detector using FK + runtime_lnks
  - methods: `append`, `compile`, `is_collided`, `get_slice`, `actors`, `get_stats`, `reset_stats`, `print_stats`

## `wrs.collider.tbd_simd_collider_x`

- **class `SIMDCollider`**
  - methods: `is_collided`

## `wrs.drivers.cobotta`
_b-CAP (ORiN2) client for DENSO controllers + a COBOTTA joint-state reader._

- **class `BCapError`** — A controller-side b-CAP failure (HRESULT with the error bit set).
- **class `BCapClient`** — Minimal synchronous b-CAP client over TCP.
  - methods: `close`, `service_start`, `service_stop`, `controller_connect`, `controller_disconnect`, `controller_getrobot`, `controller_getvariable`, `controller_execute`, `robot_getvariable`, `robot_execute`, `robot_move`, `robot_release`, `variable_getvalue`, `variable_release`
- **class `Cobotta`** — Read-only b-CAP session with one COBOTTA controller.
  - methods: `is_connected`, `connect`, `joint_angles`, `close`
- **class `CobottaMotion`** — A COBOTTA session that can also MOVE the arm.
  - methods: `is_armed`, `enable`, `disable`, `set_speed`, `move_qs`, `move_path`, `close`

## `wrs.drivers.cubemars`

- **class `CubeMarsNativeCAN`** — CubeMars servo CAN packet builder (extended CAN frame).
  - methods: `set_duty`, `set_current`, `set_cb`, `set_rpm`, `set_pos`, `set_origin`, `set_pos_spd`, `parse_feedback_8b`
- **class `CubeMarsRLinkSerial`** — R-Link serial protocol helper with optional serial session support.
  - methods: `crc16`, `build_packet`, `comm_get_values`, `comm_get_values_setup_all`, `comm_get_rotor_position_stream`, `comm_set_pos`, `comm_set_pos_spd`, `comm_set_pos_origin`, `comm_power_on_raw`, `parse_packet`, `open`, `close`, `is_open`, `send`, `read_available`, `transact`, `startup`, `set_pos`, `set_pos_spd`

## `wrs.geom.fitting`

- `convex_hull(geom)`
- `fit_plane_from_pts(pts)`

## `wrs.geom.geometry`
_Geometry container (vertices/faces/normals) and mesh-level helpers used_

- `gen_geom_from_raw(vs, fs=None)`
- `gen_cylinder_geom(length, radius=0.05, n_segs=8)` — Cylinder of total height ``length`` CENTERED on the origin along +Z
- `gen_cone_geom(length, radius=0.05, n_segs=8)`
- `gen_sphere_geom(radius=0.05, n_segs=8)`
- `gen_icosphere_geom(radius=0.05, n_subs=2)`
- `gen_arrow_geom(length, shaft_radius=wuc.ArrowSize.SHAFT_RADIUS, head_length=wuc.ArrowSize.HEAD_LENGTH, head_radius=wuc.ArrowSize.HEAD_RADIUS, n_segs=8)`
- `gen_box_geom(xyz_lengths=(0.1, 0.1, 0.1))`
- `gen_frustrum_geom(height=0.05, bottom_length=0.05, top_length=0.03)`
- `gen_capsule_geom(radius=0.05, half_length=0.1, n_segs=32)`

## `wrs.geom.loader`
_Mesh file loading: ``load_geometry(path)`` reads STL (ascii/binary) and_

- `load_geometry(path)`

## `wrs.geom.ops2d`

- `pts_in_polygon2d(pts, poly_pts)`
- `mindist_to_linesegs2d(pt, line_segs)` — pt: (2,), line_segs: array-like (M,2,2)
- `extract_boundary(fs_sub)`

## `wrs.geom.ops3d`

- `revolve(profile, n_segs=36)` — Revolve a 2D profile (r,z) around Z-axis to produce a mesh
- `subdivide_once(verts, faces)`
- `icosahedron()` — Generate vertices and faces of a unit icosahedron.
- `ray_shoot_flat(orig, dir, verts, faces, face_normals, eps=wum.eps)`
- `ray_shoot(orig, dir, geometry)`

## `wrs.geom.scaler`

- `scale_stl_files(scale=(1, 1, 1))`

## `wrs.geom.surface`

- `sample_surface(vs, fs, n_samples)`
- `segment_surface(geometry, normal_tol_deg=5.0)`

## `wrs.grasp._common`
_Shared grasp-domain helpers (depend on end-effector runtime links and_

- `build_ee_target_detector(ee, target_sobj)` — Collision detector + batch checking every link of an end effector

## `wrs.grasp.antipodal`
_Antipodal grasp planning: 2-point opposing pinch (force-closure) grasps_

- `build_grasp_rotmat_batch(ray_dirs, open_dir)` — ray_dirs: (N,3) unit vectors
- `antipodal_iter(gripper, target_sobj, density=0.02, normal_tol_deg=20, roll_step_deg=30, clearance=0.002, score_weights=(0.7, 0.3), exclude_regions=None, pre_open=0.5)` — Generator: yields (grasp, collided) -- a Grasp and its collision flag.
- `antipodal(gripper, target_sobj, density=0.02, normal_tol_deg=20, roll_step_deg=30, clearance=0.002, score_weights=(0.7, 0.3), exclude_regions=None, pre_open=0.5)` — Collects non-colliding grasps only.

## `wrs.grasp.grasp`
_The ``Grasp`` record -- a self-contained, gripper-agnostic grasp._

- **class `Grasp`** — A frozen, gripper-agnostic grasp record (see module docstring).
  - methods: `make_tcp`, `base_pose`, `transformed`, `to_dict`, `from_dict`, `from_jaw`, `from_tool`

## `wrs.grasp.monocontact`
_Monocontact: single-contact ("one contact pad") surface-approach_

- `monocontact_iter(tool, target_sobj, tcp='tip', density=0.02, roll_step_deg=90, retreat=None, approach_bias=(0.0, 0.0, 1.0), exclude_regions=None)` — Generator: yields (grasp, collided) -- a Grasp and its collision flag.
- `monocontact(tool, target_sobj, tcp='tip', density=0.02, roll_step_deg=90, retreat=None, approach_bias=(0.0, 0.0, 1.0), exclude_regions=None)` — Collects non-colliding single-contact grasps only.

## `wrs.grasp.placement`
_Stable placement poses: ``compute_stable_poses`` finds the resting_

- `compute_stable_poses(vs, fs, facets, com=None, stable_thresh=7.0)` — segs = list of list of face ids

## `wrs.grasp.polypodal`
_Polypodal: rigid N-point contact pattern matching against a mesh_

- `sample_pattern(pattern, tgt_vs, tgt_fs, n_samples, normal_tol_deg=0, distance_tol=0.001, surface_density_factor=1, exclude_regions=None)` — Sample N-point pattern placements on the target mesh surface.
- `pair_pattern(samples, tgt_vs, tgt_fs, normal_tol_deg=0, distance_tol=0.001, min_thickness=0.0, max_thickness=None)` — For every front-side (points, normals) sample in `samples`, ray-cast
- `polypodal(gripper, target_sobj, n_samples, normal_tol_deg=0, distance_tol=0.001, surface_density_factor=1, exclude_regions=None, clearance=0.0003, min_thickness=0.0, max_thickness=None, pre_open=0.5, verbose=True, return_pairs=False)` — End-to-end polypodal grasp computation.

## `wrs.grasp.reasoner`
_Grasp feasibility reasoning -- the "common grasp" kernel._

- `iter_common_gids(robot, ctx, grasps, obj_pose_list, *, tcp=None, gripper=None, chain='main', which='pre', max_solutions=1, ik_accept=None)` — Lazily yield grasps feasible at EVERY pose in ``obj_pose_list``.
- `find_feasible_gids(robot, ctx, grasps, obj_pose, **kwargs)` — Feasible grasps at a SINGLE object pose -- the one-pose special case of
- `reason_common_gids(robot, ctx, grasps, obj_pose_list, **kwargs)` — All grasps feasible at EVERY pose in ``obj_pose_list`` (the eager form of
- **class `GraspReasoner`** — Thin stateful facade over :func:`find_feasible_gids` /
  - methods: `find_feasible_gids`, `reason_common_gids`

## `wrs.grasp.serialize`
_Save / load / world-transform a list of :class:`~wrs.grasp.grasp.Grasp`._

- `save_grasps(grasps, path, gripper_name=None, object_name=None)` — Save planned grasps to a JSON file.
- `load_grasps(path)` — Load grasps previously saved by :func:`save_grasps`.
- `transform_grasps(grasps, tf)` — Map object-LOCAL grasps into the world by an object world transform.

## `wrs.manipulation.arm`
_The manipulation verbs of a single arm -- as a mixin, not a wrapper._

- **class `SingleArmManipulation`** — Manipulation verbs for one arm, delegating to the free-function cores.
  - methods: `body`, `end_effector`, `build_collider`, `recipe`, `reason_grasps`, `reachable`, `moveto`, `approach`, `depart`, `insert`, `hold`, `release`, `pick_and_place`, `iter_pick_and_place`, `pick_and_moveto`, `regrasp`
- **class `Arm`** — A per-arm manipulator OWNED by a multi-arm robot: binds the parent
  - methods: `body`

## `wrs.manipulation.pick_place`
_Pick-and-place motion planning -- move an object from a pick pose to a place_

- `gen_pick_and_place(robot, gripper, obj, grasps, pick_pose, place_pose, *, collider, tcp=None, chain='main', start_qs=None, lift_height=0.12, granularity=0.01, approach_iters=4000, transfer_iters=8000, goal_bias=0.3, constraints=())` — The FIRST feasible pick-and-place plan, or None -- the common case.
- `iter_pick_and_place(robot, gripper, obj, grasps, pick_pose, place_pose, *, collider, tcp=None, chain='main', start_qs=None, lift_height=0.12, granularity=0.01, approach_iters=4000, transfer_iters=8000, goal_bias=0.3, constraints=())` — Lazily plan home -> pick -> lift -> transfer -> place -> retreat moving

## `wrs.manipulation.recipe`
_A manipulation Recipe: compose the primitive arm verbs into one motion._

- **class `Recipe`**
  - methods: `moveto`, `linear`, `hold`, `release`, `result`, `last_qs`, `segments`, `failure`

## `wrs.manipulation.workcell`
_A multi-arm cell: several arms sharing ONE collision world._

- **class `Workcell`** — Several arms (each a ``SingleArmManipulation``) sharing one collider.
  - methods: `build_collider`, `activate`, `recipe`

## `wrs.motion.core.constraint`
_Planning constraints: state-validity predicates layered on top of collision._

- **class `Constraint`** — A state-validity predicate beyond collision.
  - methods: `is_valid`

## `wrs.motion.core.diagnosis`
_Why a planning call returned None -- an explicit, opt-in out-channel._

- `constraint_detail(constraints)` — Why a constraint rejected the last validity check: the FIRST non-None
- **class `Diagnosis`** — Where and why one planning call failed. Falsy until ``fail`` stamps it.
  - methods: `fail`

## `wrs.motion.core.motion_data`
_A plain, composable motion container._

- **class `MotionData`**
  - methods: `from_jpath`, `is_empty`, `copy`

## `wrs.motion.core.planning_context`

- **class `PlanningContext`** — Collision + bounds oracle for sampling-based planners.
  - methods: `is_state_valid`, `is_motion_valid`, `states_equal`, `enforce_bounds`, `clear_cache`, `sample_uniform`, `interpolate`, `distance`

## `wrs.motion.core.state_space`

- **class `RealVectorStateSpace`** — Simple R^n box-bounded state space, OMPL-style.
  - methods: `sample_uniform`, `distance`, `vectorized_distance`, `interpolate`, `enforce_bounds`, `satisfies_bounds`

## `wrs.motion.interpolation.cartesian`

- `linear_to_jpath(robot, start_rotmat, start_pos, goal_rotmat=None, goal_pos=None, pos_step=0.01, rot_step=np.deg2rad(2.0), ref_qs=None, chain='main', tcp='flange', ctx=None, diag=None)` — Convert Cartesian straight-line trajectory to joint trajectory using IK.
- `interp_by_step(start_rotmat, start_pos, goal_rotmat=None, goal_pos=None, pos_step=0.01, rot_step=np.deg2rad(2.0))` — Cartesian straight-line interpolation with automatic sample count.
- `interp_by_n(start_rotmat, start_pos, goal_rotmat=None, goal_pos=None, n_steps=2)` — Cartesian straight-line interpolation with fixed number of samples.

## `wrs.motion.interpolation.joint`

- `interp_by_step(start_qs, goal_qs, step=np.deg2rad(3.0))` — Joint-space straight-line interpolation with automatic sample count.
- `interp_by_n(start_qs, goal_qs, n_steps=2)` — Joint-space straight-line interpolation with fixed number of samples.
- `linear_path(start_qs, goal_qs, step=np.deg2rad(3.0), ctx=None)` — Joint-space straight-line path between two configs, optionally collision-gated.

## `wrs.motion.primitives.approach_depart`
_Approach / depart motion primitives._

- `gen_moveto(robot, ctx, planner, goal, *, tcp=None, start_qs, chain='main', ee_qpos=None, max_iters=2000, time_limit=3.0, shortcut=True, diag=None)` — Free RRT move from ``start_qs`` to ``goal`` (a config, or a pose IK'd via
- `nearest_valid_ik(robot, ctx, pos, rotmat, *, chain='main', tcp='flange', ref_qs, max_solutions=8, accept=None, diag=None)` — IK at ``(pos, rotmat)`` returning the solution nearest ``ref_qs`` (in the
- `gen_approach(robot, ctx, planner, goal_pos, goal_rotmat, *, tcp, start_qs, chain='main', pre_pos=None, pre_rotmat=None, approach_direction=None, approach_distance=0.05, granularity=0.01, ee_qpos=None, use_rrt=True, check_descent=True, max_iters=2000, ik_max_solutions=8, ik_accept=None, diag=None)` — ``start_qs`` -> pre-grasp (probabilistic) -> grasp (cartesian line).
- `gen_depart(robot, ctx, planner, start_pos, start_rotmat, *, tcp, start_qs, chain='main', depart_direction=None, depart_distance=0.05, granularity=0.01, ee_qpos=None, end_qs=None, use_rrt=False, check_retreat=True, max_iters=2000, diag=None)` — ``start_qs`` (at the grasp) -> retreat (cartesian line) -> optional park.

## `wrs.motion.probabilistic.post_processor`

- **class `PathPostProcessor`**
  - methods: `shortcut`, `densify`

## `wrs.motion.probabilistic.prm`

- **class `PRMRoadmap`**
  - methods: `add_state`, `add_edge_undirected`, `remove_edge_undirected`, `dijkstra`
- **class `PRMPlanner`**
  - methods: `solve`
- **class `LazyPRMPlanner`**
  - methods: `solve`

## `wrs.motion.probabilistic.rrt`

- `shortcut_path(path, pln_ctx, n_iter=200)`
- `densify_path(path, pln_ctx, max_step=np.pi / 12)`
- **class `RRTTree`**
  - methods: `add_node`, `nearest`, `nearest_state`, `path_from_root`
- **class `RRTConnectPlanner`**
  - methods: `solve`, `solve_iter`

## `wrs.motion.trajectory.totg`
_Time-optimal retiming of a joint waypoint path (Kunz-Stilman style)._

- `retime(q_seq, v_max, a_max, dt=0.01, corner_dt=None)` — Time-parameterize a joint waypoint path with forward/backward sweeps.

## `wrs.physics.inertial`

- `inertia_box(m, hx, hy, hz)`
- `inertia_sphere(m, r)`
- `inertia_capsule(m, r, h)`
- `inertia_from_collisions(collisions, total_mass=10.0)`

## `wrs.physics.mj_compiler`

- **class `MJCFCompiler`**
  - methods: `set_vec3`, `set_quat`, `compile_mjcf`, `compile_default`, `compile_mesh_asset`, `compile_body`, `compile_joint`, `compile_geom`, `compile_site`, `compile_actuator`

## `wrs.physics.mj_contact`

- `debug_contacts(mjenv)`
- **class `MJContactViz`**
  - methods: `clear`, `update_from_data`
- **class `MJContactForceViz`**
  - methods: `clear`, `update_from_data`

## `wrs.physics.mj_env`

- **class `MJEnv`**
  - methods: `step`, `is_collided`, `reset`, `get_timestep`, `save`, `data`, `model`, `ctrl`

## `wrs.physics.mj_naming`

- `alloc_name(prefix: str)`
- `reset_names()`

## `wrs.physics.mj_nodes`

- **class `WorldNode`**
  - methods: `compile_mjcf`
- **class `DefaultNode`**
- **class `OptionNode`**
- **class `AssetNode`**
- **class `MeshAsset`**
- **class `BodyNode`**
- **class `JointNode`**
- **class `InertialNode`**
- **class `GeomNode`**
- **class `SiteNode`** — A massless, non-colliding marker frame rigidly attached to a body.
- **class `ActuatorNode`**

## `wrs.physics.mj_runtime`

- **class `MJRuntime`**
  - methods: `step`, `forward`, `enter_cd`, `exit_cd`, `is_collided`

## `wrs.physics.mj_wrs_cvter`

- **class `MJWRSConverter`**
  - methods: `convert`, `gravity`, `timestep`

## `wrs.physics.mj_wrs_sync`

- **class `MJSynchronizer`**
  - methods: `pull_all_mecba_qpos`, `pull_all_mecba_freebase_pose`, `pull_all_sobj_pose`, `push_all_sobj_qpos`, `push_one_sobj_qpos`, `push_all_mecba_qpos`, `push_one_mecba_qpos`, `push_one_mecba_freebase_pose`

## `wrs.robots.base.kine_visualizer`

- **class `KineVisualizer`**
  - methods: `add_to_scene`, `remove_from_scene`, `refresh`

## `wrs.robots.base.mech_base`

- **class `Mounting`** — ``child`` welded to ``plnk`` at ``loc_tf``. The mount OWNS the child's
- **class `MechBase`**
  - methods: `structure`, `add_to_scene`, `remove_from_scene`, `set_pos_rotmat`, `fk`, `mount`, `unmount`, `get_solver`, `add_chain`, `chain`, `chains`, `chain_joint_limits`, `add_tcp`, `tcp`, `tcps`, `toggle_tcp`, `ik`, `ik_partial`, `clone`, `ndof`, `runtime_root_lnk`, `mounted_by`, `is_floating`, `home_qs`, `tf`, `rotmat`, `quat`, `pos`, `toggle_render_collision`, `rgba`, `rgb`, `alpha`

## `wrs.robots.base.mech_structure`

- **class `Link`**
- **class `Joint`**
  - methods: `zero_tf`, `motion_tf`
- **class `MechStruct`**
  - methods: `get_chain`, `add_lnk`, `add_jnt`, `ignore_collision`, `compile`, `collision_ignores_objs`, `n_jnts`, `n_lnks`, `compiled`
- **class `FlatMechStructure`** — flat representation of RobotStructure for efficient computation
  - methods: `resolve_all_qs`, `is_active_jnt`

## `wrs.robots.base.tcp`

- **class `TCP`** — A local frame (link + offset) used as an IK target point.
  - methods: `tf`, `pos`, `rotmat`, `set_loc_tf`, `set_loc_rotmat_pos`, `copy`

## `wrs.robots.base.urdf_loader`

- `auto_scan_packages(main_file, search_root)` — Scan main_file and auto-detect "$(find pkg)" patterns,
- `load_robot_from_xacro(main_file, base_dir=None, mappings=None)` — Load a robot URDF from a xacro or urdf file.
- `urdf_to_mechstruct(urdf, urdf_dir, collision_type=None, res_dir=None, mesh_resolver=None)` — Convert a urdf_parser_py URDF (Robot) object into a one MechStruct.

## `wrs.robots.base.kine.anaik`

- **class `AnaIKBase`** — Analytic IK base class (returns all solutions).
  - methods: `ik`, `ik_all`, `get_rotmat_from_fk`
- **class `S456X12`**
  - methods: `ik_all`
- **class `P234X56`**
  - methods: `ik_all`

## `wrs.robots.base.kine.geoik`

- `is_parallel(h1, h2, eps=1e-06)`
- `line_line_distance(p1, d1, p2, d2)` — distance between two 3D lines:
- `line_point_distance(p, l0, d)`
- `line_line_midpoint(p1, d1, p2, d2, eps=1e-09)`
- `estimate_wrist_center(o1, h1, o2, h2, o3, h3, eps_intersect=1e-05)`
- `is_intersecting(p1, d1, p2, d2, eps=1e-05)`
- `analyze_decomposition(chain, eps_parallel=1e-06, eps_intersect=1e-05)`
- **class `KineInfo`**

## `wrs.robots.base.kine.kinematic_chain`

- **class `KinematicChain`** — get a base_link to tip_link chain
  - methods: `embed_active_qs`, `extract_active_qs`, `n_active_jnts`, `n_jnts`

## `wrs.robots.base.kine.numik`

- **class `NumIKSolver`** — Solver for one chain
  - methods: `fk`, `ik`, `ik_partial`

## `wrs.robots.base.kine.numik_sel`

- **class `SELIKSolver`**
  - methods: `build_and_save_cvt_database`, `query_seeds`, `ik`, `ik_partial`

## `wrs.robots.base.kine.poe_ana_solver`

- **class `Ana6RSWSolver`**
  - methods: `ik`

## `wrs.robots.base.kine.random_ik_test`

- `make_robot(name)`
- `make_solver(name, robot, default_solver)`
- `rand_active_qs(chain)`

## `wrs.robots.base.kine.ikgeo.rand_py`

- `hat(k)`
- `rand_angle()`
- `rand_mat(size=1)`
- `rand_vec(size=3)`
- `rand_normal_vec(size=3)`
- `rand_normal_mat(size=1)`
- `rand_perp_normal_vec(inp)`
- `rot(k, theta)`

## `wrs.robots.base.kine.ikgeo.sp1_lib`

- `sp1_setup(p1, p2, k, theta)`
- `sp1_run(p1, p2, k, tol=1e-05)`
- `sp1_setup_LS(p1, p2, k)`
- `sp1_error(p1, p2, k, theta)`

## `wrs.robots.base.kine.ikgeo.sp2E_lib`

- `sp2E_setup(p0, p1, p2, k1, k2)`
- `sp2E_run(p0, p1, p2, k1, k2)`
- `sp2E_error(p0, p1, p2, k1, k2, theta1, theta2)`

## `wrs.robots.base.kine.ikgeo.sp2_lib`

- `sp2_setup(p1, p2, k1, k2)`
- `sp2_setup_LS(p1, p2, k1, k2)`
- `sp2_run(p1, p2, k1, k2)`
- `sp2_error(p1, p2, k1, k2, theta1, theta2)`

## `wrs.robots.base.kine.ikgeo.sp3_lib`

- `sp3_setup(p1, p2, k)`
- `sp3_setup_LS(p1, p2, k)`
- `sp3_run(p1, p2, k, d)`
- `sp3_error(p1, p2, k, d, theta)`

## `wrs.robots.base.kine.ikgeo.sp4_lib`

- `sp4_setup(p, k, h)`
- `sp4_setup_LS(p, k, h)`
- `sp4_run(p, k, h, d)`
- `sp4_error(p, k, h, d, theta)`

## `wrs.robots.base.kine.ikgeo.sp5_lib`

- `sp5_setup(p0, p1, p2, p3, k1, k2, k3)`
- `cone_polynomials(p0_i, k_i, p_i, p_i_s, k2)`
- `sp5_run(p0, p1, p2, p3, k1, k2, k3)`
- `sp5_error(p0, p1, p2, p3, k1, k2, k3, theta1, theta2, theta3)`

## `wrs.robots.base.kine.ikgeo.sp6_lib`

- `sp6_setup(H, K, P)`
- `solve_2_ellipse_numeric(xm1, xn1, xm2, xn2)`
- `sp6_run(H, K, P, d1, d2)`
- `sp6_error(H, K, P, theta1, theta2, d1, d2)`

## `wrs.robots.end_effectors.ee_mixins`

- **class `EndEffectorMixin`** — Uniform object-holding for any actuated end effector (gripper, hand).
  - methods: `hold`, `release`
- **class `GripperMixin`** — A parallel-jaw gripper: closes via a single scalar ``set_opening``, and
  - methods: `open`, `close`, `set_opening`, `grip_at`, `grasp_center_tcp`, `qpos`, `mode`
- **class `PointMixin`**
  - methods: `activate`, `deactivate`, `touch_at`, `hold`, `release`, `is_activated`
- **class `DexHandMixin`** — Behavior for a multi-finger dexterous hand (a MechBase EE).
  - methods: `grasp_spec`, `open_hand`, `open`, `grip`, `pinch`, `tripod`, `power`, `grasp_at`, `pinch_at`, `power_at`, `as_jaw`
- **class `JawView`** — A parallel-jaw view of a dexterous hand, bound to one opposition
  - methods: `runtime_lnks`, `qpos`, `add_to_scene`, `rgb`, `alpha`, `clone`, `set_opening`, `open_dir_at`, `grasp_center_at`, `grasp_center_tcp`, `grip_at`

## `wrs.robots.end_effectors.denso.cvr038_gripper.cvr038_gripper`

- `prepare_ms()`
- **class `CVR038Gripper`** — Parallel-jaw gripper: a MechBase + GripperMixin. No chain / ik (motion is
  - methods: `set_opening`, `clone`

## `wrs.robots.end_effectors.fr3_gripper.fr3_gripper`

- `prepare_ms()`
- **class `FR3Gripper`**
  - methods: `set_opening`, `clone`

## `wrs.robots.end_effectors.linkerbot.o6.o6`

- `prepare_mechstruct(side, collision_type=wuc.CollisionType.MESH)` — Load one side of the Linkerbot O6 dexterous hand from its standalone
- **class `O6Left`**
- **class `O6Right`**

## `wrs.robots.end_effectors.onrobot.or_2fg7.or_2fg7`

- `prepare_ms()`
- **class `OR2FG7`**
  - methods: `set_opening`, `clone`

## `wrs.robots.end_effectors.onrobot.or_sd.or_sd`

- **class `ORSD`** — Minimal OnRobot ScrewDriver implementation.

## `wrs.robots.end_effectors.openarm_gripper.oa_gripper`

- `prepare_ms()`
- **class `OAGripper`**
  - methods: `set_opening`, `clone`

## `wrs.robots.end_effectors.robotiq.rtq2f85.rtq2f85`

- `prepare_ms()`
- **class `Rtq2F85`**
  - methods: `set_opening`, `clone`

## `wrs.robots.end_effectors.xhand.xhand_right`

- `prepare_mechstruct(collision_type=wuc.CollisionType.MESH)` — Load the XHand right (12-dof dexterous hand) from its URDF into a
- **class `XHandRight`** — XHand right: a 12-dof dexterous hand as a mountable MechBase EE with the
  - methods: `grasp_spec`, `goto_given_conf`, `rand_conf`

## `wrs.robots.humanoids.linx.l1.l1`

- `prepare_mechstruct(collision_type=wuc.CollisionType.MESH)` — Load the Linx L1/H0602 upper-body URDF into a MechStruct, via the shared
- **class `L1`** — Linx L1 upper-body humanoid, bare (no end-effectors). Arms tip at the
  - methods: `lnk`, `jnt`
- **class `L1O6`** — Linx L1 with two dexterous Linkerbot O6 hands mounted on the arm
  - methods: `clone`

## `wrs.robots.manipulators.denso.cvr038.cvr038`

- `prepare_mechstruct()`
- `cvr038_with_gripper(rotmat=None, pos=None, jaw_width=0.03)` — Convenience factory: CVR038 arm with the stock COBOTTA gripper attached.
- **class `CVR038`** — 6-DOF single-arm: a MechBase configured with one root->tip chain and a

## `wrs.robots.manipulators.denso.cvr038.ik.cvr038_pencil_ik`

- **class `CVR038PencilIK`** — CVR038-specific semi-analytic IK.
  - methods: `ik_all`

## `wrs.robots.manipulators.denso.cvr038.ik.gen_q4_pencil_coeffs`
_Offline generator for q4_pencil_coeffs.py (NOT used at runtime)._

- `main()`

## `wrs.robots.manipulators.denso.cvr038.ik.q4_pencil_coeffs`
_GENERATED by gen_q4_pencil_coeffs.py - do not edit by hand._

- `eval_coeffs(px, pz, vx, vy, vz)` — Return (c3_vals, lu_vals) aligned with C3_KEYS / LU_KEYS.

## `wrs.robots.manipulators.denso.cvr038.ik.q4_pencil_solve`

- `q4_roots_pencil_fast(px, py, pz, vx, vy, vz, tol=1e-08)` — Closed-form-coefficient pencil for the fixed CVR038 geometry.

## `wrs.robots.manipulators.fanuc.crx5ia.crx5ia`

- `prepare_mechstruct()`
- **class `CRX5ia`**

## `wrs.robots.manipulators.fanuc.crx5ia.ik.crx5ia_analytic_ik`

- **class `CRX5iaAnalyticIK`** — CRX-5iA analytic IK (returns all solutions).
  - methods: `ik_all`

## `wrs.robots.manipulators.franka.fr3.fr3`

- `prepare_mechstruct()`
- `fr3_with_hand(rotmat=None, pos=None, jaw_width=0.08)` — Convenience factory: FR3 arm with the stock Franka Hand attached.
- **class `FR3`**

## `wrs.robots.manipulators.kawasaki.rs007l.rs007l`

- `prepare_mechstruct()`
- **class `RS007L`**

## `wrs.robots.manipulators.openarm.openarm`

- `link(prefix, name, idx, mesh_dir, rgb=wuc.ExtendedColor.DEEP_GRAY)`
- `joint(prefix, name, parent_lnk, child_lnk)`
- `prepare_arm_ms(prefix='lft')`
- `prepare_bdy_ms()`
- **class `OALft`**
- **class `OARgt`**
- **class `OABody`** — Single body link (openarm_body_link0).
- **class `OpenArm`**
  - methods: `add_to_scene`, `remove_from_scene`, `rotmat`, `pos`, `set_pos_rotmat`

## `wrs.robots.manipulators.universal_robots.ur3.ur3`

- `prepare_mechstruct()`
- **class `UR3`**

## `wrs.robots.manipulators.xarm.lite6.lite6`

- `prepare_mechstruct()`
- **class `Lite6`**

## `wrs.robots.vehicle.xytheta`

- `prepare_mechstruct()`
- **class `XYThetaRobot`**

## `wrs.scene.collision_shape`

- **class `CollisionShape`**
  - methods: `to_render_model`, `clone`, `geom`, `loc_tf`, `quat`, `rotmat`, `pos`, `aabb`
- **class `SphereCollisionShape`**
  - methods: `fit_from_geom`, `clone`, `to_render_model`, `radius`, `aabb`
- **class `CapsuleCollisionShape`**
  - methods: `fit_from_geom`, `clone`, `to_render_model`, `radius`, `half_length`, `aabb`
- **class `CylinderCollisionShape`** — A flat-ended cylinder (MuJoCo native ``cylinder`` geom). Same fit as the
  - methods: `fit_from_geom`, `clone`, `to_render_model`, `radius`, `half_length`, `aabb`
- **class `AABBCollisionShape`**
  - methods: `fit_from_geom`, `clone`, `to_render_model`, `half_extents`, `aabb`
- **class `OBBCollisionShape`**
  - methods: `fit_from_geom`, `clone`, `to_render_model`, `half_extents`, `aabb`
- **class `PlaneCollisionShape`**
  - methods: `fit_from_geom`, `clone`, `to_render_model`, `normal`, `aabb`
- **class `MeshCollisionShape`**
  - methods: `clone`, `to_render_model`, `file_path`, `geom`, `aabb`
- **class `CvxHullCollisionShape`**
  - methods: `fit_from_geom`, `clone`

## `wrs.scene.geometry_ops`
_Mesh geometry operations on raw (vertices, faces) arrays: surface_

- `revolve(profile, n_segs=36)` — Revolve a 2D profile (r,z) around Z-axis to produce a mesh
- `subdivide_once(verts, faces)`
- `icosahedron()` — Generate vertices and faces of a unit icosahedron.
- `triangle_areas(vs, fs)` — Per-face triangle areas of a mesh. vs (V,3), fs (F,3) -> (F,).
- `sample_count_from_area(vs, fs, density)` — Number of surface samples for a target spacing ``density`` (one
- `ray_triangles_batch_far(origins, directions, v0, v1, v2, eps=1e-06)` — Moller-Trumbore ray-triangle intersection (batch version).
- `sample_surface(vs, fs, n_samples)`
- `segment_surface(geometry, normal_tol_deg=15.0)`
- `convex_hull(geom)`
- `ray_shoot_flat(orig, dir, verts, faces, face_normals, eps=wum.eps)`
- `ray_shoot(orig, dir, geometry)`
- `clip_mesh(tgt_vs, tgt_fs, exclude_regions)` — Cut convex `exclude_regions` out of the mesh.

## `wrs.scene.render_model`

- **class `RenderModel`** — rotmat and pos of model is for transforming local geometries
  - methods: `clone`, `vrgbs`, `rgb`, `alpha`, `quat`, `pos`, `rotmat`, `loc_tf`, `set_pos_rotmat`

## `wrs.scene.render_model_primitive`

- `gen_mesh_rmodel(vs, fs, rgb, alpha=1.0)`
- `gen_pcd_rmodel(vs, vrgbs, alpha=1.0)`
- `gen_cylinder_rmodel(length=0.1, radius=0.05, n_segs=8, rotmat=None, pos=None, rgb=wuc.BasicColor.DEFAULT, alpha=1.0)` — Gen cylinder render model from (0,0,0) to (0,0,length).
- `gen_cone_rmodel(length=0.1, radius=0.05, n_segs=8, rgb=wuc.BasicColor.DEFAULT, alpha=1.0)` — Gen cone render model from (0,0,0) to (0,0,length).
- `gen_sphere_rmodel(radius=0.05, n_segs=8, rgb=wuc.BasicColor.DEFAULT, alpha=1.0)` — Gen sphere render model at (0,0,0).
- `gen_icosphere_rmodel(radius=0.05, n_subs=2, rgb=wuc.BasicColor.DEFAULT, alpha=1.0)` — Gen icosphere render model at (0,0,0).
- `gen_box_rmodel(xyz_lengths=(0.1, 0.1, 0.1), rgb=wuc.BasicColor.DEFAULT, alpha=1.0)` — Gen box render model centered at (0,0,0).
- `gen_frustrum_rmodel(height=0.05, bottom_length=0.05, top_length=0.03, rotmat=None, pos=None, rgb=wuc.BasicColor.DEFAULT, alpha=1.0)`
- `gen_arrow_rmodel(length, shaft_radius, head_length, head_radius, n_segs, rotmat=None, pos=None, rgb=wuc.BasicColor.DEFAULT, alpha=1.0)` — Gen arrow render model from (0,0,0) to (0,0,length).

## `wrs.scene.scene`

- **class `Scene`**
  - methods: `add`, `remove`, `sobjs`, `lnks`, `mecbas`

## `wrs.scene.scene_object`

- **class `SceneObject`** — A posed, renderable body.
  - methods: `from_file`, `set_pos_rotmat`, `pos`, `rotmat`, `quat`, `tf`, `add_to_scene`, `remove_from_scene`, `add_visual`, `add_collision`, `clone`, `set_inertia`, `collision_group`, `collision_affinity`, `mounted_by`, `is_floating`, `rgb`, `alpha`, `rgba`, `inrtmat`, `com`, `mass`

## `wrs.scene.scene_object_primitive`

- `cylinder(spos=(0, 0, 0), epos=(0.01, 0.01, 0.01), radius=0.05, segments=8, rgb=wuc.BasicColor.DEFAULT, alpha=1.0, **kwargs)`
- `dashed_cylinder(spos=(0, 0, 0), epos=(0.01, 0.01, 0.01), radius=0.05, len_solid=None, len_interval=None, segments=8, rgb=wuc.BasicColor.DEFAULT, alpha=1.0, **kwargs)`
- `cone(spos=(0, 0, 0), epos=(0.01, 0.01, 0.01), radius=0.05, segments=8, rgb=wuc.BasicColor.DEFAULT, alpha=1.0, **kwargs)`
- `sphere(pos=(0, 0, 0), radius=0.05, segments=8, rgb=wuc.BasicColor.DEFAULT, alpha=1.0, **kwargs)`
- `icosphere(pos=(0, 0, 0), radius=0.05, subdivisions=2, rgb=wuc.BasicColor.DEFAULT, alpha=1.0, **kwargs)`
- `box(pos=(0, 0, 0), xyz_lengths=(0.1, 0.1, 0.1), rotmat=None, rgb=wuc.BasicColor.DEFAULT, alpha=1.0, **kwargs)`
- `linsegs(segs, radius=0.001, srgbs=None, alpha=1.0)` — segs: (N,2,3), srgb: None | scalar | (3,) | (N,3)
- `arrow(spos=np.zeros(3), epos=np.ones(3) * 0.01, shaft_radius=wuc.ArrowSize.SHAFT_RADIUS, head_radius=wuc.ArrowSize.HEAD_RADIUS, head_length=wuc.ArrowSize.HEAD_LENGTH, n_segs=8, rgb=wuc.BasicColor.DEFAULT, alpha=1.0, **kwargs)`
- `dashed_arrow(spos=np.zeros(3), epos=np.ones(3) * 0.01, shaft_radius=wuc.ArrowSize.SHAFT_RADIUS, head_radius=wuc.ArrowSize.HEAD_RADIUS, head_length=wuc.ArrowSize.HEAD_LENGTH, len_solid=None, len_interval=None, n_segs=8, rgb=wuc.BasicColor.DEFAULT, alpha=1.0, **kwargs)`
- `frame_from_tf(tf, length_scale=1.0, radius_scale=1.0, n_segs=8, color_mat=wuc.CoordColor.RGB, alpha=1.0, **kwargs)` — Draw a coordinate frame at the pose given by a 4x4 transform.
- `frame(pos=np.zeros(3), rotmat=np.eye(3), length_scale=1.0, radius_scale=1.0, n_segs=8, color_mat=wuc.CoordColor.RGB, alpha=1.0, **kwargs)`
- `dashed_frame(pos=np.zeros(3), rotmat=np.eye(3), length_scale=1.0, radius_scale=1.0, len_solid=None, len_interval=None, n_segs=8, color_mat=wuc.CoordColor.RGB, alpha=1.0, **kwargs)`
- `plane(pos=(0, 0, 0), normal=wuc.StandardAxis.Z, size=(100.0, 100.0), thickness=0.001, rgb=wuc.BasicColor.GRAY, alpha=1.0)`
- `point_cloud(vs, vrgbs, alpha=1.0)` — Build a point-cloud SceneObject from per-vertex positions and colors.
- `frustrum(base_center=(0, 0, 0), top_center=(0, 0, 0.05), bottom_length=0.05, top_length=0.03, rgb=wuc.BasicColor.DEFAULT, alpha=1.0, **kwargs)`
- `mesh(vs, fs, collision_type=None, is_floating=False, rgb=wuc.BasicColor.DEFAULT, alpha=1.0, **kwargs)` — Build a SceneObject from user-specified vertices/faces.

## `wrs.utils.constant`

- **class `BasicColor`**
- **class `ExtendedColor`**
- **class `Tab20`**
- **class `ALPHA`**
- **class `AxisColor`**
- **class `CoordColor`**
- **class `StandardAxis`**
- **class `StandardCoord`**
- **class `ArrowSize`**
- **class `ForceArrowSize`**
- **class `JntType`**
- **class `CollisionType`**
- **class `CollisionGroup`**
- **class `CollisionMatrix`** — Default collision permission table: which groups each group collides with.
- **class `DefaultPhy`**

## `wrs.utils.decorator`

- `mark_dirty(dirty_method_name)`
- `lazy_update(dirty_flag_name, update_method_name)`
- `check_positive(func)`
- `readonly_view(func)`

## `wrs.utils.gpu_device`
_The one wgpu adapter/device for the process._

- `get_adapter()`
- `get_device()`

## `wrs.utils.helper`
_Misc utility helpers: array pretty-printing, random color generation._

- `print_arr(name, arr, precision=8)`
- `rand_rgb()`

## `wrs.utils.math`
_Transform & rotation math: rotmat_from_* (axangle, quat, euler, rotvec,_

- `vec(*args)`
- `axis_from_name(name)`
- `parse_axis_constraints(axis_constraints)` — Parse local-to-target axis constraints into unit vector pairs.
- `axis_angle_error(src_axis, tgt_axis)` — Smallest angle between two axes in radians.
- `rotmat_from_axis_constraints(axis_constraints, ref_rotmat=None, n_iter=4)` — Build a rotmat that satisfies axis constraints as closely as possible.
- `rotmat_from_axangle(ax, angle)`
- `rotmat_from_quat(quat)`
- `rotmat_from_normal(normal, up=(0, 0, 1))` — rotation matrix from normal vector,  normal is the z axis
- `rotmat_from_normalandpoints(facet_normal, facet_first_pnt, facet_second_pnt)` — TODO: deprecate this function
- `rotmat_from_euler(ai, aj, ak, order='sxyz')` — :param ai: radian
- `rotmat_from_rotvec(v)`
- `rotmat_between_vecs(v1, v2)` — Rotation mapping v1 onto v2. Scalar (3,) inputs return a (3, 3) matrix;
- `rotmat_average(rot_mats)`
- `rotmat_slerp(rotmat0, rotmat1, n)`
- `tf_from_axangle(ax, angle)` — homogeneous matrix from the given axis and angle
- `tf_from_pos_rotmat(pos=None, rotmat=None)`
- `tf_from_rotvec(pos=np.zeros(3), rotvec=np.ones(3))` — build a (4,4) transformation matrix from position and rotation vector
- `tf_from_quat(quat)` — (4,4) homogeneous transformation matrix from a quaternion (XYZW).
- `tf_from_quat_pos(quat, pos)` — (4,4) homogeneous transformation matrix from a quaternion (XYZW) and a
- `tf_inverse(tf)` — compute the inverse of a homogeneous transformation matrix
- `tf_average(tf_list, bandwidth=10)` — TODO: tf list or (n,4,4) array
- `transform_points_by_tf(tf, pnts)` — do homotransform on a point or an array of points using pos
- `interplate_pos_rotmat(start_pos, start_rotmat, goal_pos, goal_rotmat, granularity=0.01)` — :param start_info: [pos, rotmat]
- `interplate_pos_rotmat_around_circle(circle_center_pos, circle_normal_ax, radius, start_rotmat, end_rotmat, granularity=0.01)` — :param circle_center_pos:
- `interpolate_vectors(start_vector, end_vector, granularity)` — :param start_vector:
- `quat_from_rotmat(rotmat)`
- `quat_from_axangle(ax, angle)`
- `average_quaternions(quats)` — Mean of a set of quaternions (N, 4), XYZW in and out.
- `quat_from_euler(ai, aj, ak, order='sxyz')` — Quaternion (XYZW) from Euler angles and axis sequence.
- `rotvec_from_rotmat(rotmat)`
- `euler_from_quat(quat, order='sxyz')` — Euler angles from a quaternion (XYZW).
- `euler_from_rotmat(rotmat, order='sxyz')`
- `quaternion_multiply(quaternion1, quaternion0)` — Compose two rotations (XYZW): apply ``quaternion0`` first.
- `quaternion_conjugate(quaternion)` — Conjugate of a UNIT quaternion (XYZW) -- the inverse rotation.
- `quaternion_inverse(quaternion)` — Inverse of a quaternion (XYZW).
- `quaternion_real(quaternion)` — Real (scalar) part of a quaternion (XYZW) -- the LAST component.
- `quaternion_imag(quaternion)` — Imaginary (vector) part of a quaternion (XYZW) -- the first three.
- `slerp_quat(quat0, quat1, fraction)` — Spherical linear interpolation between two quaternions (XYZW).
- `rand_quaternion()` — Uniform random unit quaternion (XYZW).
- `rand_rotmat()` — Return uniform random rotation matrix.
- `skew(vec)`
- `orth_vec(vec, toggle_unit=True)` — compute a vector orthogonal to the given 3D vector
- `frame_from_normal(n)` — compute a coordinate frame whose 3rd column is the (unit) vector n
- `closest_point_between_lines(p1, d1, p2, d2)` — line1=p1 + t*d1, line2=p2 + s*d2
- `intersect_lines(lines, cond_thresh=1000000000000.0)` — least-squares intersection point of multiple 3D lines
- `rel_pose(pos0, rotmat0, pos1, rotmat1)` — relpos of rot1, pos1 with respect to rot0 pos0
- `regulate_angle(lowerbound, upperbound, jntangles)` — change the range of armjnts to [lowerbound, upperbound]
- `unit_vec(vec, axis=-1, return_length=True)` — :param vec: (1,n)
- `angle_between_vecs(vec1, vec2)` — :param vec1: (1,3)
- `angle_between_2d_vecs(vec1, vec2)` — return the angle from ved1 to vec2, with signs
- `delta_rotvec_between_rotmats(src_rotmat, tgt_rotmat)` — compute the rotvec from src_rotmat to tgt_rotmat
- `diff_between_poses(src_pos, src_rotmat, tgt_pos, tgt_rotmat)` — compute the error between the given tcp and tgt_tcp
- `cosine_between_vecs(v1, v2)`
- `axangle_between_rotmat(rotmati, rotmatj)`
- `quaternion_to_axangle(quaternion)` — :param quaternion: XYZW
- `pos_average(pos_list, bandwidth=10)` — average a list of pos_vec (1x3)
- `pos_quat_from_tf(tf, quat_order='xyzw')`
- `intersect_planes(p1, n1, p2, n2, tol=1e-06)`
- `gen_2d_spiral_points(max_radius: float=0.002, radial_granularity: float=0.0001, tangential_granularity: float=0.0003, toggle_origin: bool=False)` — gen spiral curve
- `gen_3d_spiral_points(pos: npt.NDArray=np.zeros(3), rotmat: npt.NDArray=np.eye(3), max_radius: float=0.002, radial_granularity: float=0.0001, tangential_granularity: float=0.0003, toggle_origin: bool=False)` — gen spiral curve
- `gen_regpoly(radius, nedges=12)`
- `gen_2d_isosceles_verts(nlevel, edge_length, nedges=12)`
- `gen_2d_equilateral_verts(nlevel, edge_length)`
- `gen_3d_isosceles_verts(pos, rotmat, nlevel=5, edge_length=0.001, nedges=12)`
- `gen_3d_equilateral_verts(pos, rotmat, nlevel=5, edge_length=0.001)`
- `get_aabb(pointsarray)` — get the axis aligned bounding box of nx3 array
- `compute_pca(nparray)` — :param nparray: nxd array, d is the dimension
- `transform_data_pcv(data, random_rot=True)` — :param data:
- `fit_plane(points)` — :param points: nx3 nparray
- `project_point_to_plane(point, plane_center, plane_normal)`
- `project_vector_to_vector(vector1, vector2)`
- `distance_point_to_edge(point, edge_start, edge_end)` — compute the minimum distance from a point to a line segment.
- `min_distance_point_edge_list(contact_point, edge_list)` — compute the minimum distance between a point and a list of nested edge lists.
- `points_obb(pointsarray, toggledebug=False)` — applicable to both 2d and 3d pointsarray
- `gaussian_ellipsoid(pointsarray)` — compute a 95% percent ellipsoid axes_mat for the given points array
- `random_rgba(toggle_alpha_random=False)` — randomize a 1x4 list in range 0-1
- `consecutive(nparray1d, stepsize=1)` — find consecutive sequences from an array
- `null_space(npmat)`
- `to_homogeneous(pos)` — append 1 to pos
- `reflection_homomat(point, normal)` — Return matrix to mirror at plane defined by point and normal vector.
- `reflection_from_homomat(homomat)` — Return mirror plane point and normal vector from reflection homomat
- `projection_homomat(point, normal, perspective=None, pseudo=False)` — Return matrix to project onto plane defined by point and normal.
- `affine_matrix_from_points(v0, v1, shear=True, scale=True, use_svd=True)` — Return affine transform matrix to register two point sets.
- `rotmat_from_look_at(pos, look_at, up)` — Constructs a camera rotation matrix from pos to look_at
- `area_weighted_pca(verts, faces, eps=eps)` — area weighted pca for a mesh defined by verts and faces
- `wrap_to_pi(angle)` — wrap angle to [-pi, pi]
- `clamp(x, lo, hi)` — clamp x to [lo, hi]
- `ensure_right_handed(rotmat)` — ensure the given rotmat is right-handed
- `ensure_rotmat(rotmat=None)`
- `ensure_pos(pos=None)`
- `ensure_vec(vec, length=None)`
- `ensure_tf(tf=None)`
- `ensure_rgb(rgb=None)`

## `wrs.utils.scheduler`
_Timed callbacks for a viewer loop, ticked once per frame._

- **class `Scheduler`** — Timed callbacks, called as ``fn(dt, *args, **kwargs)``.
  - methods: `schedule_interval`, `schedule_once`, `unschedule`, `tick`

## `wrs.viewer.key`
_Key symbols: ASCII for printable keys, X11 keysyms for the rest._

- `symbol_from_name(name)` — rendercanvas key name -> key symbol, or None if unmapped.

## `wrs.viewer.protocol`
_Scene -> wire._

- `pack(header: Dict[str, Any], arrays: List[bytes])` — One binary frame from a header and the blobs its offsets refer to.
- `unpack(frame: bytes)` — Inverse of :func:`pack`: the header and a view on the blob.
- `iter_scene_models(scene)` — Walk the scene as (model_id, model, owner) -- no serialization.
- `model_entry(model, model_id: str)` — A drawable with no arrays of its own: which geometry, and what colour.
- `geometry_entry(model)` — That geometry's arrays, as (metadata, {field: raw bytes}).
- `describe(pairs, known_geoms)` — (model_id, model) pairs -> the entries a message carries.
- `scene_message(msg_type, models, geometries, camera=None, remove=None, replay=False)` — ``scene_init`` or ``scene_delta``: any geometry the far end is missing,
- `split_geometries(header, blob)` — Frame -> the (metadata, {field: bytes}) pairs geometry_entry makes.
- `transform_arrays(snapshot)` — A snapshot as (ids, (N, 16) float32) -- one column-major matrix a row.
- `transform_message(ids, matrices)` — Poses for the ids given: ids in the header, matrices in one contiguous
- `split_transforms(header, blob)` — A pose frame -> {id: raw 64 bytes}, for a cache that has to survive

## `wrs.viewer.server`
_The hub: one long-lived process, one port, serving the page and relaying_

- `serve(host='127.0.0.1', port=DEFAULT_PORT, idle_timeout=IDLE_TIMEOUT, auto_open=True)`
- `main()`
- **class `Hub`** — Fan-out from one publisher to any number of viewers.
  - methods: `watch_idle`, `on_publish`, `on_view`

## `wrs.viewer.world`
_The world a script builds its scene in, drawn by the browser page._

- **class `World`** — Same surface as the old native World, minus the window.
  - methods: `set_scene`, `set_caption`, `schedule_interval`, `schedule_once`, `stop`, `stop_after`, `event`, `dispatch`, `is_key_pressed`, `is_key_pressed_edge`, `close`, `run`

## `wrs.viewer.web_ui.constant`
_Web UI constants, following wrs.utils.constant's class/member convention._

- **class `Anchor`** — Panel placement; values remain strings in the browser protocol.

## `wrs.viewer.web_ui.manager`
_Named panels, a default panel and UI event routing._

- **class `UIManager`** — World.ui: a default panel plus named panels with independent controls.
  - methods: `add_panel`, `remove_panel`

## `wrs.viewer.web_ui.panel`
_One Python-owned panel: control state, snapshots and main-loop callbacks._

- **class `UIPanel`** — Define a small control panel and publish immutable state snapshots.
  - methods: `configure`, `show`, `hide`, `add_button`, `add_slider`, `add_label`, `add_select`, `set_value`, `set_enabled`, `remove`

## `wrs.viewer.web_ui.protocol`
_UI message contract and pure validation, without sockets, DOM or callbacks._

- `valid_id(value)` — Whether a panel/control/event/session ID is a nonempty bounded string.
- `is_ui_event(payload)` — Check the event envelope; the addressed panel validates its value.
- `finite_number(value)` — Accept real finite numbers, excluding booleans and numeric strings.
- `slider_value(control, value)` — Validate bounds and snap to the native range input's step lattice.
- `select_value(control, value)` — Accept only one of the dropdown's string options.
